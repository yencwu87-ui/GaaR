# WB141 operational candidate

Open `Run_WB141.command` or run `bash Run_WB141.command` in Terminal. It sets up the project Python environment and checks what is ready.

This is a separate checkpoint. Keep your existing WB136/WB140 installation and its evidence/ledgers unchanged until you deliberately migrate them.

The included configuration will report BLOCKED: it contains no real model configuration, signing keys, authority approvals or operating evidence. This is the expected safe result, not a successful assessment.

Read `ge_repo/WB141_OPERATIONS.md` for configuration and execution. Read `ge_repo/WB141_VERIFICATION.md` for the exact proof and remaining gates. Historical synthetic launchers remain available for compatibility but are not WB141 production proof.
