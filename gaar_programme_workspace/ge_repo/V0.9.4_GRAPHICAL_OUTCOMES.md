# v0.9.4 — Graphical Outcomes Surface

## What changed
- Added graphical outcome status tiles to the Outcomes tab.
- Added deterministic posture summary metrics: adequate/remediate/escalate/defer.
- Added an Outcome -> Capability coverage heatmap using the existing authoritative `evaluate_all()` projection.
- Added deterministic outcome drill-down with mapped MAS/MGF elements, evidence modes, blockers, capability status, and next action.
- Retained the existing audit table as a collapsed fallback for dense review/export-oriented inspection.

## Governance boundary
All graphics are projections only. They do not create or mutate governance records, and they do not replace MAS/MGF requirements or the append-only event log.

## Verification
- `python -m py_compile app.py`: passed
- `pytest -q test_outcomes_wb054.py test_outcomes_platform_wb055.py`: 12 passed
- `python -m compileall -q .`: passed
